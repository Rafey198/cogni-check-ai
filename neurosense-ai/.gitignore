# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# ---- Virtual environments ----
.env
.venv
env/
venv/
ENV/

# ---- Secrets / environment ----
.env.*
*.local

# ---- Streamlit ----
.streamlit/secrets.toml

# ---- Trained models (regenerated by the Train Model page) ----
# The app retrains and recreates models/trained_model.pkl on demand, so the
# binary artifact does not need to be version-controlled. Uncomment the next
# line if you prefer to ignore it; it is kept tracked by default so the demo
# works immediately after deployment.
# models/*.pkl

# ---- OS / editor / temp files ----
.DS_Store
Thumbs.db
*.tmp
*.temp
*.log
.ipynb_checkpoints/
.vscode/
.idea/
